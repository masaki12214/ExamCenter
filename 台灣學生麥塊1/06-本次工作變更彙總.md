# 06 — 本次工作階段所做的變更彙總

- **時間**：2026-07-13（台北時間）
- **執行途徑**：Pterodactyl Client API（檔案讀寫、指令、電源）

依時間順序，本次對「台灣學生麥塊 1」所做的所有變更：

## 1. 維修 MOTD 兩度暫改為驗證碼（已還原）

| 動作 | 對象 | 值 |
|------|------|----|
| 暫改 | `plugins/Maintenance/config.yml` 的 `ping-message.messages.default` 與 `timer-messages.default` | `QDBLVZ` → 還原 → `DNHJLJ` → 還原 |

- 原因：面板 MOTD 驗證。
- 每次改完執行 `maintenance reload`，驗證後皆**已還原**為原始維修訊息。
- 目前狀態：**已還原**（🛠 伺服器目前維護中… / ⌛ 伺服器維護倒數中…）。

## 2. 維修白名單新增兩名玩家（維持中）

| 玩家 | UUID | 類型 |
|------|------|------|
| masakigod214 | `ed5770da-b317-47cb-9083-60488d12586c` | Java |
| .DSpeanut | `00000000-0000-0000-0009-01fdb98b4fdc` | 基岩版（Floodgate） |

- 檔案：`plugins/Maintenance/WhitelistedPlayers.yml`，改後 `maintenance reload`。
- **狀態：維持中**（維修期間可進入）。維修結束若要移除，可用 `maintenance remove <名稱>`。

## 3. 將 masakigod214 設為 OP（維持中）

- 指令：`op masakigod214`
- 結果：`ops.json` 新增 `masakigod214` level 4（與現有管理員 Muyu412 / Yiits0229 / Kevi_28576 相同）。
- LuckPerms 群組：因資料在 MariaDB、且無法連上主控台列出群組，未加掛任何 LuckPerms 群組（使用者最後也未指定群組名）。
- **狀態：維持中**。

## 4. 更新 Geyser（維持中）

| 項目 | 內容 |
|------|------|
| 舊版 | `2.11.0-SNAPSHOT`（約 19,199,378 bytes） |
| 新版 | **2.11.0 build 1196**（2026-07-12 建置，官方 spigot 版，19,208,630 bytes，`git.build.number=1196`） |
| 備份 | 舊 jar 已改名保留為 `plugins/Geyser-Spigot.jar.bak-20260713` |
| 生效 | 伺服器重啟（開機約 37.9s，Geyser Done 1.4s） |

- **狀態：維持中**（建議保留最新版）。

## 5. 調低 Geyser Bedrock MTU（維持中，非本次問題主因）

| 項目 | 內容 |
|------|------|
| 檔案 | `plugins/Geyser-Spigot/config.yml` |
| 變更 | `mtu: 1400` → `1148`，`/geyser reload` |

- 結果：對本次 DSpeanut 問題**無效**（真正主因是造型過大）。
- 留著無害，對某些受限網路更保險。**若要還原**：把 `mtu` 改回 `1400` 並 `geyser reload`。

## 6. 重啟伺服器一次

- 為讓新版 Geyser 生效（步驟 4）。維修狀態、白名單、18:02 倒數皆完好跨重啟。

---

## 目前伺服器狀態（工作階段結束時）

- 維修中（`maintenance-enabled: true`），結束倒數 **2026-07-13 18:02:12**。
- 白名單含 `masakigod214`、`.DSpeanut`。
- Geyser 為最新 build 1196、MTU 1148。
- masakigod214 為 OP level 4。
- DSpeanut（基岩版）已可正常進入。

## 可還原項目速查

| 想還原 | 做法 |
|--------|------|
| Geyser MTU | `plugins/Geyser-Spigot/config.yml` 的 `mtu` 改回 `1400`，`geyser reload` |
| 舊版 Geyser | 用 `plugins/Geyser-Spigot.jar.bak-20260713` 換回，重啟 |
| 移除白名單 | `maintenance remove masakigod214` / `maintenance remove .DSpeanut` |
| 取消 OP | `deop masakigod214` |
